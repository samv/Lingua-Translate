# Automatically added by dh_installdocs
if [ \( "$1" = "upgrade" -o "$1" = "remove" \) -a -L /usr/doc/liblingua-translate-perl ]; then
	rm -f /usr/doc/liblingua-translate-perl
fi
# End automatically added section
